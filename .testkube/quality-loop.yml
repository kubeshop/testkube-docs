# Quality Loop event map (replaces .github/workflows/prettier.yml).
#
# | workflow       | when                         | branches      | listens to |
# |----------------|------------------------------|---------------|------------|
# | docs-prettier  | pull_request + push to main  | any PR base;  | all files  |
# |                |                              | push `main`   |            |
#
# TestWorkflow `docs-prettier` is created on the GitHub integration, not in this folder.
workflows:
  - name: docs-prettier
    on: [pull_request]
    required: true
  - name: docs-prettier
    on: [push]
    branches: [main]
    required: true
